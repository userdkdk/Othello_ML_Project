# Runtime Specs

## 파일 역할
- `project-structure.md`
  - `src/engine`, `src/training`, `src/api`, `frontend` 계층 경계
  - 테스트 경로와 import 방향
- `web-runtime-spec.md`
  - FastAPI 엔드포인트와 분리된 웹 UI 계약
- `training-dashboard-spec.md`
  - 학습 제어 API, 상태 조회 payload, checkpoint 비교 UI/API 계약
